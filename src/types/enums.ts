export enum RaceStatus {
  PENDING = "pending",
  RUNNING = "running",
  PAUSED = "paused",
  FINISHED = "finished",
}

export enum ScheduleStatus {
  NOT_STARTED = "not-started",
  RUNNING = "running",
  FINISHED = "finished",
}
